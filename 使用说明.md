# 春意墨生 — AI书法创作系统 使用说明

## 一、系统简介

**春意墨生**是基于深度学习（GAN）与风格解耦技术的AI书法创作系统。支持6种经典书法风格，覆盖行书、楷书、隶书、篆书、草书五大书体。

### 技术架构

```
用户输入文本 + 风格选择
    ↓
后端API（FastAPI）→ 三级生成策略
    ↓
[1] 字库匹配优先 → Calli-Tongji字库高清字形
    ↓ 未命中
[2] GAN模型推理 → StyleEncoder + ContentEncoder + DualBranchGenerator
    ↓ 模型未加载
[3] Canvas模拟渲染 → 系统字体 + 后处理效果
    ↓
宣纸纹理 + 红色印章 → PNG输出
```

---

## 二、环境配置

### 2.1 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 |
| Python | 3.8 - 3.12 |
| GPU | NVIDIA RTX 3050 4GB+（可选，用于GPU推理加速） |
| 显存 | 建议8GB+（GPU模式） |
| 硬盘 | 至少5GB可用空间 |

### 2.2 依赖安装

**方式一：自动安装（推荐）**

双击运行 `install_cuda_pytorch.bat`，脚本会自动安装：
- CUDA 12.x 驱动
- PyTorch (CUDA版)
- OpenCV
- FastAPI + Uvicorn

**方式二：手动安装**

```bash
# 安装PyTorch（GPU版）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# 安装其他依赖
pip install fastapi uvicorn python-multipart opencv-python opencv-contrib-python Pillow numpy
```

### 2.3 环境验证

运行 `check_env.py` 检查环境是否就绪：

```bash
python check_env.py
```

正常输出应显示：
```
✅ Python: 3.x.x
✅ PyTorch: 2.x.x (CUDA可用/仅CPU)
✅ OpenCV: 4.x.x
✅ FastAPI: 已安装
```

---

## 三、启动系统

### 3.1 启动后端服务

**方法一：使用启动脚本**

双击运行 `start_training_fast.bat` 或在命令行执行：

```bash
python algorithm\server\app.py
```

**方法二：直接运行**

```bash
cd D:\书法春
python algorithm/server/app.py
```

服务启动成功后会显示：
```
INFO:     Uvicorn running on http://0.0.0.0:8080
INFO:     Application startup complete.
[ModelManager] 初始化完成，设备: cuda/cpu
```

### 3.2 访问前端

服务启动后，打开浏览器访问：

```
http://localhost:8080
```

或直接双击打开项目目录中的 `index.html` 文件（离线模式，使用Canvas模拟渲染）。

---

## 四、功能使用

### 4.1 创作书法作品

**Step 1：输入文本**

在左侧文本框中输入要创作的书法内容。支持：
- 单字（如："龙"）
- 词语（如："上善若水"）
- 诗句（如："春眠不觉晓"）

快捷输入：点击"诗句"或"经典"按钮可快速填充示例文本。

**Step 2：选择布局**

- **竖排/横排**：点击布局切换按钮
- **字间距**：拖动滑块调整（0-100）
- **行间距**：拖动滑块调整（0-100）
- **字号大小**：拖动滑块调整（30-120px）
- **纸幅比例**：竖幅 / 方幅 / 横幅

**Step 3：选择风格**

支持两种风格选择模式：

| Tab | 风格 | 说明 |
|-----|------|------|
| 经典书家 | 米芾、赵孟頫、褚遂良、乙瑛碑、邓石如、怀素 | 复刻历代名家笔法 |
| 五大书体 | 楷书、行书、草书、隶书、篆书 | 按书体类型选择 |

**Step 4：调整风格参数**

| 参数 | 范围 | 效果 |
|------|------|------|
| 笔法粗细 | 0-100 | 控制笔画粗细 |
| 墨色浓淡 | 0-100 | 控制墨色深浅 |
| 结字疏密 | 0-100 | 控制字体紧凑程度 |
| 飞白效果 | 0-100 | 添加飞白笔触效果 |

**Step 5：生成与下载**

1. 点击**「生成书法」**按钮，等待生成完成
2. 点击**「下载作品」**保存PNG格式图片

### 4.2 查看风格库

点击导航栏「作品集」，可浏览6位书法家的代表作品风格展示。

### 4.3 查看技术架构

页面底部展示系统的技术架构流程图，说明从文本输入到书法输出的完整数据流。

---

## 五、支持的书法风格

| 风格 | 书体 | 朝代 | 风格特点 |
|------|------|------|----------|
| 米芾 | 行书 | 宋代 | 沉着痛快，八面出锋，风樯阵马 |
| 赵孟頫 | 楷书 | 元代 | 圆润秀美，流畅自然，遒媚姿媚 |
| 褚遂良 | 楷书 | 唐代 | 清朗秀劲，疏瘦劲健，灵动飘逸 |
| 乙瑛碑 | 隶书 | 汉代 | 婉畅飘逸，圆浑沉着，遒劲古拙 |
| 邓石如 | 篆书 | 清代 | 圆转匀称，刚健婀娜，篆法精绝 |
| 怀素 | 草书 | 唐代 | 狂放飘逸，如骤雨旋风，挥毫泼墨 |

---

## 六、模型训练

### 6.1 完整训练（6风格）

```bash
python algorithm/train/run_train.py
```

**配置参数**：
- 训练分辨率：128×128
- 批大小：4-8（根据显存调整）
- 训练轮数：60 epoch
- GPU训练速度：约143秒/epoch

### 6.2 单风格快速训练

```bash
# 米芾单风格训练（30 epoch）
python algorithm/train/train_mifu.py
```

### 6.3 继续训练

从已有checkpoint继续训练（降低学习率微调）：

```bash
python algorithm/train/continue_train.py
```

### 6.4 训练数据

训练数据位于 `algorithm/data/` 目录：
- `米芾/` — 米芾行书训练样本
- `赵孟頫/` — 赵孟頫楷书训练样本
- `褚遂良/` — 褚遂良楷书训练样本
- `乙瑛碑/` — 乙瑛碑隶书训练样本
- `邓石如/` — 邓石如篆书训练样本
- `怀素/` — 怀素草书训练样本

### 6.5 模型检查点

训练完成后，模型保存在 `checkpoints/` 目录。

---

## 七、常见问题

### Q1: 启动报错 `ModuleNotFoundError: No module named 'fastapi'`

**解决方案**：安装依赖包

```bash
pip install fastapi uvicorn python-multipart
```

### Q2: 模型加载失败

正常现象！系统设计了三级降级策略：
1. 字库匹配优先
2. GAN模型推理（需要GPU显存充足）
3. Canvas模拟渲染（系统字体）

即使GAN模型未加载，系统仍可正常使用Canvas模式生成书法。

### Q3: 生成速度慢

- **GPU模式**：约5-10秒/次生成
- **CPU模式**：约30-60秒/次生成
- **Canvas模式**：约1-3秒/次生成

如需加速，请确保已安装CUDA版PyTorch。

### Q4: 字库覆盖率

系统使用Calli-Tongji字库，常见汉字覆盖率约70-80%。字库中没有的字会自动使用Canvas模拟渲染。

### Q5: 生成的图片如何使用

下载的PNG图片可以直接：
- 打印输出
- 制作成书法作品
- 用于PPT/文档配图
- 分享到社交媒体

---

## 八、快捷命令

| 命令 | 说明 |
|------|------|
| `python check_env.py` | 检查环境配置 |
| `python algorithm/server/app.py` | 启动后端服务 |
| `python algorithm/train/run_train.py` | 开始训练（6风格） |
| `python algorithm/train/train_mifu.py` | 米芾单风格训练 |

---

## 九、联系与反馈

如遇问题，请提供以下信息：
1. 错误提示完整内容
2. 环境配置（`python check_env.py` 输出）
3. 操作系统和Python版本

---

*春意墨生 — 让AI唤醒千年笔墨*
