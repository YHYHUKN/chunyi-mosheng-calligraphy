# 春意墨生 — AI书法创作系统 项目代码说明书

## 一、项目概述

### 1.1 项目简介

**春意墨生**是一个基于深度学习（GAN）与风格解耦技术的AI书法创作系统。系统融合计算机视觉与传统书法艺术，支持米芾、赵孟頫、褚遂良、乙瑛碑、邓石如、怀素六种经典风格，覆盖行书、楷书、隶书、篆书、草书五大书体。

### 1.2 技术栈

| 层次 | 技术 |
|------|------|
| 深度学习框架 | PyTorch (CUDA) |
| 后端服务 | FastAPI + Uvicorn |
| 前端 | 纯 HTML / CSS / JavaScript（无框架） |
| 图像处理 | OpenCV、PIL (Pillow) |
| 字体渲染 | Windows 系统书法字体 |
| 开发环境 | Windows、Python 3.x、CUDA 12.x |
| GPU | NVIDIA RTX 3050 4GB |

### 1.3 模型架构

采用**双分支风格解耦 GAN**，核心组件：

```
StyleEncoder → 风格隐向量 (B, 128)
ContentEncoder → 内容特征图 (B, 256, 8, 8)
    ↓                              ↓
DualBranchGenerator ← 特征融合（AdaIN + 门控）
    ↓
Discriminator → 真假判别 + 风格分类
```

### 1.4 模型配置参数

| 参数 | 值 | 说明 |
|------|-----|------|
| image_size | 128 | 训练分辨率（128×128） |
| style_dim | 128 | 风格隐空间维度 |
| content_dim | 256 | 内容特征维度 |
| batch_size | 4~8 | 批大小（根据GPU显存调整） |
| num_epochs | 5~60 | 训练轮数 |
| lr_g | 2e-4 | 生成器学习率 |
| lr_d | 4e-4 | 判别器学习率 |
| GPU训练速度 | ~143s/epoch | 每epoch约2.5分钟 |

---

## 二、目录结构

```
d:\书法春\
├── index.html                      # 前端入口页面
├── start.bat                       # 启动脚本（启动FastAPI服务）
├── design-philosophy.md            # 设计哲学文档
│
├── assets/                         # 前端静态资源
│   ├── css/
│   │   └── styles.css              # 全站样式表
│   ├── js/
│   │   ├── spring-animation.js     # 春天树木萌芽动画
│   │   ├── calligraphy-renderer.js # 书法Canvas渲染器
│   │   └── app.js                  # 应用交互逻辑控制器
│   └── images/                     # 图片资源目录（空）
│
├── algorithm/                      # 后端算法模块
│   ├── __init__.py                 # 包初始化
│   ├── server/
│   │   ├── __init__.py
│   │   └── app.py                  # FastAPI 后端服务（核心）
│   ├── models/
│   │   ├── __init__.py
│   │   ├── style_encoder.py        # 风格编码器 + 内容编码器 + 自注意力
│   │   ├── generator.py            # 双分支生成器 + 判别器
│   │   └── losses.py               # 多维损失函数
│   ├── train/
│   │   ├── __init__.py
│   │   ├── train.py                # 训练器基础类（CalligraphyTrainer）
│   │   ├── run_train.py            # 完整训练脚本（6风格）
│   │   ├── run_train_fast.py       # 快速训练变体
│   │   ├── run_train_zhao_fast.py  # 赵孟頫快速训练
│   │   ├── train_mifu.py           # 米芾单风格训练脚本
│   │   ├── continue_train.py       # 从checkpoint继续训练
│   │   └── test_model_output.py    # 模型输出质量测试
│   ├── utils/
│   │   ├── __init__.py
│   │   ├── preprocessing.py        # 数据预处理（分割/骨架/增强/质量检测）
│   │   └── style_utils.py          # 风格参数预测/插值/笔画渲染器
│   └── data/                       # 训练数据目录
│       ├── 米芾/                   # 米芾行书训练样本
│       ├── 赵孟頫/                 # 赵孟頫楷书训练样本
│       ├── 褚遂良/                 # 褚遂良楷书训练样本
│       ├── 乙瑛碑/                 # 乙瑛碑隶书训练样本
│       ├── 邓石如/                 # 邓石如篆书训练样本
│       └── 怀素/                   # 怀素草书训练样本
│
├── checkpoints/                    # 模型检查点存储
├── 书法字库/                       # Calli-Tongji 字库（离线字体图）
│
├── check_env.py                    # 环境检测脚本
├── debug_ckpt.py                   # Checkpoint调试工具
├── debug_direct_gen.py             # 直接生成调试
├── install_cuda_pytorch.bat        # CUDA/PyTorch安装脚本
└── start_training*.bat             # 多个训练启动脚本
```

---

## 三、前端模块详细说明

### 3.1 index.html — 前端入口页面

**路径**: `index.html`

**功能**: 系统的单页面入口，包含完整的HTML结构。

**页面结构**:

| 区域 | ID | 功能 |
|------|-----|------|
| 顶部导航 | `mainHeader` | 品牌标识 + 页面锚点导航（创作/作品集/学习） |
| Hero区 | `heroSection` | 系统介绍、核心卖点、统计数据、滚动书法预览 |
| 创作工作区 | `workspace` | 左侧控制面板 + 右侧实时预览画布 |
| 风格库展示 | `styleGallery` | 六位书法家风格卡片展示 |
| 技术架构展示 | `architecture` | 流程图展示双分支生成架构 |
| 底部 | `site-footer` | 品牌信息、链接 |

**创作工作区控制面板包含**:
- **文本输入**: 文本框 + 快捷文本按钮（诗句/经典）
- **布局调整**: 横排/竖排切换、字间距/行间距/字号滑块、纸幅比例选择
- **风格选择**: 经典书家（6位）+ 五大书体 Tab切换，卡片式选择
- **风格参数**: 笔法粗细/墨色浓淡/结字疏密/飞白效果 四个滑块

**脚本加载顺序**: `spring-animation.js` → `calligraphy-renderer.js` → `app.js`

---

### 3.2 assets/css/styles.css — 全站样式表

**路径**: `assets/css/styles.css`

**功能**: 定义系统全部视觉样式，约1083行。

**核心设计系统**:

- **CSS变量体系**: 在 `:root` 中定义完整的色彩、间距、圆角、阴影、字体变量
  - 主色调：春绿 `#5a7247`、水墨色 `#2c2418`、金色 `#b8954a`、粉色 `#c4887a`
  - 背景：宣纸色 `#faf6ee`、次背景 `#f0ebe1`
  - 字体：衬线 `Noto Serif SC`、无衬线 `Noto Sans SC`
- **动画系统**: `fadeUp`、`fadeIn`、`breathe`、`shimmer`、`gentleFloat`、`scrollUnfurl`
- **组件样式**: 按钮（primary/ghost/outline/text/icon/toolbar，6种尺寸）、卡片、面板、滑块、标签页
- **布局**: Hero双列布局、Workspace网格（控制面板320-400px + 自适应预览）、响应式三断点（1024px/640px）
- **无障碍**: `prefers-reduced-motion` 媒体查询支持

---

### 3.3 assets/js/calligraphy-renderer.js — 书法Canvas渲染器

**路径**: `assets/js/calligraphy-renderer.js`

**功能**: 前端书法模拟渲染引擎，使用Canvas API模拟毛笔书法效果。

**核心类 `CalligraphyRenderer`**:

| 方法 | 功能 |
|------|------|
| `constructor(canvas)` | 初始化渲染器，绑定Canvas |
| `setStyle(styleKey)` | 切换书法风格（支持11种预设） |
| `render(options)` | 主渲染函数，根据选项绘制完整作品 |
| `animateRender(options)` | 带逐字显现动画的渲染 |
| `drawChar(char, x, y, scale)` | 渲染单个汉字 |
| `drawPaper()` | 绘制宣纸纹理背景 |
| `drawGrid()` | 绘制可选网格线 |
| `drawSeal(x, y, size)` | 绘制红色印章 |
| `applyFlyingWhite(...)` | 模拟飞白效果（destination-out擦除） |
| `applyInkVariation(...)` | 模拟墨色浓淡变化（径向渐变叠加） |
| `calculateCanvasSize()` | 根据文本和布局自动计算画布尺寸 |
| `static renderCardPreview(...)` | 在风格卡片上绘制预览字 |
| `static renderHeroCalligraphy(...)` | 在Hero区域绘制装饰书法 |

**风格预设系统 `STYLES`**（17种）:

| Key | 书法家 | 书体 | 字体 | 特点 |
|-----|--------|------|------|------|
| `mifu` | 米芾 | 米芾行书 | 华文行楷 | 倾斜0.04，墨色偏淡，对比1.2 |
| `zhaomf` | 赵孟頫 | 赵体楷书 | 华文楷体 | 端正，对比1.15 |
| `chushl` | 褚遂良 | 褚体楷书 | 华文楷体 | 笔画偏细，高对比1.35 |
| `yybei` | 乙瑛碑 | 隶书 | 华文隶书 | 笔画最粗1.25，加粗700 |
| `dengsr` | 邓石如 | 篆书 | 华文仿宋 | 低对比0.85，墨色浓0.95 |
| `huaisu` | 怀素 | 怀素草书 | 华文行楷 | 最倾斜0.06，墨色最淡0.7 |
| + 5种通用书体 + 6种兼容旧Key | | | | |

每个风格包含参数：`font`、`weight`、`brushWidth`、`inkDensity`、`charDensity`、`contrast`、`skewX`、`color`

---

### 3.4 assets/js/app.js — 应用交互逻辑控制器

**路径**: `assets/js/app.js`

**功能**: 前端应用的主控制器，管理全局状态、事件绑定、API交互。

**全局状态 `state`**:

```javascript
{
  currentStyle: 'mifu',      // 当前选中风格
  currentLayout: 'vertical', // 布局方向
  currentRatio: 'square',    // 纸幅比例
  text: '春眠不觉晓',        // 输入文本
  showGrid: false,           // 是否显示网格
  brushWeight: 50,           // 笔法粗细 (0-100)
  inkDensity: 50,            // 墨色浓淡 (0-100)
  charDensity: 50,           // 结字疏密 (0-100)
  flyingWhite: 0,            // 飞白效果 (0-100)
  charSpacing: 20,           // 字间距 (px)
  lineSpacing: 16,           // 行间距 (px)
  fontSize: 64               // 字号 (px)
}
```

**核心函数**:

| 函数 | 功能 |
|------|------|
| `init()` | 初始化渲染器、绑定所有事件 |
| `updatePreview()` | 实时更新预览画布 |
| `bindTextEvents()` | 文本输入 + 快捷按钮事件 |
| `bindLayoutEvents()` | 布局控制（横竖排/纸幅/间距/字号） |
| `bindStyleEvents()` | 风格Tab切换 + 卡片选择 |
| `bindParamEvents()` | 风格参数滑块绑定 |
| `bindToolbarEvents()` | 工具栏（放大/缩小/格线） |
| `bindActionEvents()` | 生成按钮 + 下载按钮 + 自定义风格 |
| `apiGenerate()` | 调用后端API生成书法（含降级逻辑） |
| `renderAllCardPreviews()` | 绘制所有风格卡片预览 |
| `renderGalleryPreviews()` | 绘制风格库展示区预览 |
| `showToast(message)` | Toast消息提示 |
| `scrollToSection(id)` | 平滑滚动到指定区域 |

**后端降级策略**: 当调用 `/api/generate` 返回的图片 >99.5% 白色像素时，自动判定为空白图，回退到前端Canvas渲染。

---

### 3.5 assets/js/spring-animation.js — 春天树木萌芽动画

**路径**: `assets/js/spring-animation.js`

**功能**: 全屏背景粒子动画，模拟春天树木从发芽到花朵绽放的过程。

**动画阶段**（共5个阶段，约7.5秒完成）:

| 阶段 | 时间 | 效果 |
|------|------|------|
| GROUND | 0-1s | 地面线出现 |
| TRUNK | 1-3s | 树干生长（贝塞尔曲线） |
| BRANCHES | 3-5.5s | 分形树枝展开（递归5层） |
| BLOSSOMS | 5.5-7.5s | 花朵绽放 + 绿叶出现 |
| STEADY | 7.5s+ | 花瓣粒子持续飘落 |

**技术实现**:
- 分形树生成：递归分支，每层3个子分支，随机角度和收缩比
- 花朵：5瓣椭圆 + 花蕊，4种粉色随机
- 飘落粒子：椭圆形花瓣，带正弦摇摆和旋转，生命周期衰减
- 树干：贝塞尔曲线绘制，线性渐变填充

---

## 四、后端模块详细说明

### 4.1 algorithm/server/app.py — FastAPI后端服务

**路径**: `algorithm/server/app.py`

**功能**: 系统的核心后端服务，提供REST API，管理模型加载和推理。

**服务配置**:
- 框架：FastAPI
- 端口：`0.0.0.0:8080`
- 启动方式：`python algorithm/server/app.py`
- CORS：全开放

**数据模型**:

```python
class GenerateRequest:      # 生成请求
    text, style_name, layout, char_spacing, line_spacing,
    font_size, paper_ratio, brush_weight, ink_density,
    char_density, flying_white

class StyleInfo:            # 风格信息
    name, display_name, script_type, description, era, sample_preview

class GenerateResponse:     # 生成响应
    success, image_base64, generation_time, style_used
```

**核心类 `ModelManager`（单例模式）**:

| 方法 | 功能 |
|------|------|
| `__init__()` | 初始化设备、风格库、字库缓存 |
| `_init_char_library()` | 扫描 Calli-Tongji 字库，构建 {风格名: {字: 路径}} 映射 |
| `_init_style_library()` | 初始化6种预置风格的元数据 |
| `load_models(dir)` | 加载训练好的 StyleEncoder/ContentEncoder/Generator |
| `generate(request)` | **核心生成入口**，三级降级策略 |
| `_match_from_library(chars, style)` | 从字库匹配字符（OpenCV裁切+居中） |
| `_compose_mixed(...)` | 混合合成（字库图 + Canvas渲染） |
| `_generate_real(request)` | GAN模型推理（风格向量+骨架→生成） |
| `_generate_simulated(request)` | Canvas模拟渲染（无模型时的降级方案） |
| `_render_single_char(...)` | 单字渲染（8步后处理） |
| `_apply_flying_white(...)` | 方向性飞白条纹 |
| `_apply_brush_edge(...)` | 随机形态学边缘腐蚀 |
| `_apply_stroke_ends(...)` | 起笔收笔笔锋渐变 |
| `_get_style_params(name, req)` | 风格参数合成（默认+用户调节） |
| `_find_calligraphy_font(name)` | 风格→系统字体映射 |

**三级生成降级策略**:

```
1. 字库匹配优先 → 扫描 Calli-Tongji 字库
   ↓ 未命中
2. GAN模型推理 → StyleEncoder + ContentEncoder + DualBranchGenerator
   ↓ 模型未加载或输出空白
3. Canvas模拟渲染 → PIL字体渲染 + 多层后处理
```

**API路由**:

| 路由 | 方法 | 功能 |
|------|------|------|
| `/` | GET | 返回前端页面 (index.html) |
| `/api/styles` | GET | 获取所有可用风格列表 |
| `/api/generate` | POST | 生成书法作品（返回base64图片） |
| `/api/upload_style` | POST | 上传自定义风格样本 |
| `/api/health` | GET | 系统健康检查（模型状态/设备/风格列表） |
| `/assets` | 静态 | 静态资源服务 |

**风格→系统字体映射**:

| 风格 | 映射字体 | 备选 |
|------|----------|------|
| 米芾 | STXINGKA.TTF（华文行楷） | 华文楷体 |
| 赵孟頫 | STKAITI.TTF（华文楷体） | — |
| 褚遂良 | STKAITI.TTF（华文楷体） | — |
| 乙瑛碑 | STLITI.TTF（华文隶书） | — |
| 邓石如 | STZHONGS.TTF（华文中宋） | — |
| 怀素 | STXINGKA.TTF（华文行楷） | — |

---

### 4.2 algorithm/models/style_encoder.py — 风格/内容编码器

**路径**: `algorithm/models/style_encoder.py`

**功能**: 定义风格编码器、内容编码器和自注意力模块。

#### StyleEncoder

**输入**: 书法灰度图 `(B, 1, H, W)` → **输出**: 风格隐向量 `(B, 128)`

**网络结构**:
```
输入 → [Conv→IN→LeakyReLU]×5（下采样5次，256→128→64→32→16→8）
     → SelfAttention（增强全局风格特征）
     → AdaptiveAvgPool2d(1) → FC(512→256→ReLU→128)
     → L2归一化
```

**关键方法**:
- `interpolate(style1, style2, alpha)` — 两个风格之间线性插值
- `mix(styles, weights)` — 多风格加权混合

#### ContentEncoder

**输入**: 字形骨架图 `(B, 1, H, W)` → **输出**: 内容特征图 `(B, 256, H', W')`

**网络结构**:
```
输入 → [Conv→IN→LeakyReLU]×4（下采样4次）
     → Conv→IN→LeakyReLU（保持空间尺寸）
```

#### SelfAttention

**功能**: SAGAN风格的自注意力模块，增强全局风格特征提取。

```
Q, K = Conv1x1(C → C//8), V = Conv1x1(C → C)
Attention = Softmax(Q·K^T)
Output = γ × V·Attention + x  （γ可学习，初始为0）
```

---

### 4.3 algorithm/models/generator.py — 双分支生成器 + 判别器

**路径**: `algorithm/models/generator.py`

**功能**: 系统核心网络架构。

#### AdaptiveInstanceNorm (AdaIN)

**功能**: 风格注入的核心机制。用风格向量通过FC层生成 γ 和 β，调制特征图的均值和方差。

```
x → InstanceNorm → γ × x + β
其中 [γ, β] = FC(style_vector).chunk(2)
```

#### StyleModulatedConvBlock

生成器的基本构建单元：

```
输入 → 上采样(2×) → AdaIN → LeakyReLU → Conv3×3 → AdaIN
```

#### DualBranchGenerator

**核心架构** — 双分支逐层融合：

| 分支 | 功能 | 流程 |
|------|------|------|
| 内容分支 | 字形结构 | content_features → 4层StyleModulatedConv上采样（16→32→64→128→256） |
| 风格分支 | 笔法墨法 | style_vector → FC → Reshape → 4层ConvTranspose上采样（8→16→32→64→128） |

**融合机制** (`FeatureFusionBlock`):
```
concat(content_feat, style_feat) → Conv1×1 → Sigmoid → gate
fused = gate × content + (1-gate) × style_proj → Conv3×3
```

**输出层**:
```
fused → Conv→IN→LeakyReLU → Conv1×1 → Tanh → [-1, 1]
```

#### Discriminator

**双分支判别器**:

| 分支 | 功能 | 结构 |
|------|------|------|
| 真假判别 | 判断生成真实性 | 共享特征提取(5层Conv下采样) → AvgPool → FC(512→1) |
| 风格分类 | 判断是否符合目标风格 | 共享特征提取 → AvgPool → FC(512→num_styles) |

---

### 4.4 algorithm/models/losses.py — 多维损失函数

**路径**: `algorithm/models/losses.py`

**功能**: 针对书法生成优化的综合损失函数。

#### VGGFeatureExtractor

使用预训练 VGG19（ImageNet权重）提取多层特征：
- `relu1_2` — 低层纹理
- `relu2_2` — 中层结构
- `relu3_4` — 高层语义

灰度图自动扩展为3通道。

#### CalligraphyLoss — 综合损失

**总损失公式**:

```
L_total = λ1·L_adv + λ2·L_perc + λ3·L_style + λ4·L_content + λ5·L_style_cls
```

| 损失项 | 权重 | 计算方式 | 作用 |
|--------|------|----------|------|
| L_adv | 1.0 | LSGAN（MSE） | 提升生成真实感 |
| L_perc | 10.0 | VGG特征空间L1 | 匹配笔画细节纹理 |
| L_style | 5.0 | Gram矩阵MSE | 匹配笔法墨法统计特征 |
| L_content | 1.0 | 像素L1 + Sobel边缘L1 | 保持字形结构准确 |
| L_style_cls | 1.0 | CrossEntropy | 确保风格分类正确 |

**辅助方法**:
- `_gram_matrix(x)` — 计算 Gram 矩阵（风格的统计表示）
- `_edge_loss(gen, target)` — Sobel 边缘检测一致性损失

---

### 4.5 algorithm/train/ — 训练模块

#### run_train.py — 完整训练脚本（6风格）

**路径**: `algorithm/train/run_train.py`

**功能**: 系统的主训练脚本，支持6种风格的联合训练。

**配置**: image_size=128, batch_size=8, 5 epoch（调试用）

**流程**:
1. 扫描 `algorithm/data/` 下6个风格目录
2. 提取骨架（Zhang-Suen细化）并缓存为 `.npz`
3. 数据增强（旋转±8°、弹性形变、亮度变化、随机模糊）
4. 训练循环（判别器→生成器，交替优化）
5. 保存 checkpoint + `style_map.json` + `train_log.json`

**Dataset `CalligraphyDS`**:
- 缓存机制：预处理后的图片和骨架以 `.npz` 格式缓存
- 增强：`aug_factor` 控制每张原图生成的增强副本数
- 输出格式：`image(1,128,128)`, `skeleton(1,128,128)`, `style_label`，归一化到 [-1,1]

#### train_mifu.py — 米芾单风格训练

**路径**: `algorithm/train/train_mifu.py`

**功能**: 仅训练米芾单一风格，快速验证模型效果。

**配置**: 128px, 30 epochs, batch 8, aug×6（100张→~700张），约3小时

**与 run_train.py 的区别**: STYLE_MAP 只含 `{"米芾": 0}`, NUM_STYLES=1

#### continue_train.py — 继续训练

**路径**: `algorithm/train/continue_train.py`

**功能**: 从已有 checkpoint 恢复训练，降低学习率进行微调。

**配置**: 降低 lr_g=5e-5, lr_d=1e-4, aug_factor=5, 继续训练160 epoch

**流程**:
1. 自动查找最佳 checkpoint（优先 `*_best.pth`）
2. 加载权重
3. 用更低的学习率重建优化器
4. 从断点 epoch 继续训练

#### train.py — 训练器基础类

**路径**: `algorithm/train/train.py`

**功能**: 定义 `CalligraphyTrainer` 基类，包含完整的训练/推理/保存/加载逻辑。

**关键方法**:
- `train_epoch(dataloader)` — 单epoch训练（D-step + G-step交替）
- `generate_samples(text, style)` — 推理生成
- `save_checkpoint(epoch, dir)` — 保存检查点
- `load_checkpoint(path)` — 加载检查点

#### test_model_output.py — 模型输出测试

**路径**: `algorithm/train/test_model_output.py`

**功能**: 快速测试模型输出质量，支持随机噪声输入和真实图片输入两种测试。

---

### 4.6 algorithm/utils/preprocessing.py — 数据预处理工具

**路径**: `algorithm/utils/preprocessing.py`

**功能**: 书法数据集的预处理、增强、质量筛选工具集。

**核心类 `DataPreprocessor`**:

| 方法 | 功能 |
|------|------|
| `load_and_preprocess(path)` | 图片读取→灰度→去噪→二值化→裁切→缩放到256×256 |
| `extract_skeleton(image)` | Zhang-Suen细化算法提取笔画骨架 |
| `split_characters(image, layout)` | 整幅作品→单字分割（支持竖排/横排） |
| `augment(image, skeleton, n)` | 数据增强（旋转/弹性形变/墨色/模糊，每种n个变体） |
| `quality_check(image, skeleton)` | 质量评分（清晰度+笔画密度+骨架连通性） |

**核心类 `CalligraphyDataset`**:

PyTorch Dataset 实现，自动加载数据目录结构 `data_path/书法家名/书体/*.jpg`，支持训练/验证分割。

**数据结构 `CalligraphySample`**:

```python
@dataclass
class CalligraphySample:
    image: np.ndarray       # 原始图片
    skeleton: np.ndarray    # 字形骨架（二值图）
    style_label: int        # 风格标签索引
    calligrapher: str       # 书法家名称
    script_type: ScriptType # 书体类型
    char: str               # 对应汉字
    quality_score: float    # 质量评分
```

---

### 4.7 algorithm/utils/style_utils.py — 风格参数工具

**路径**: `algorithm/utils/style_utils.py`

**功能**: 风格参数可解释化、风格插值、笔画渲染与作品合成。

**核心类 `StyleAttributePredictor`**:

从风格隐向量预测5个可解释的书法属性：
- brush_weight（笔法粗细）
- ink_density（墨色浓淡）
- char_density（结字疏密）
- stroke_speed（行笔速度）
- flying_white（飞白程度）

网络结构：`Linear(128→64→ReLU→32→ReLU→5)→Sigmoid`

**核心类 `StyleInterpolator`**:

- `interpolate(style1, style2, alpha)` — 风格线性插值
- `mix_styles(names, weights)` — 多风格加权混合
- `adjust_attribute(vector, attr, delta)` — 属性定向调整
- `find_nearest_styles(vector, k)` — 最近邻风格检索

**核心类 `StrokeRenderer`**:

| 方法 | 功能 |
|------|------|
| `create_paper_texture(h, w)` | 生成宣纸纹理（噪点+纤维线条） |
| `compose_work(chars, layout, ...)` | 单字合成完整作品（竖排从右到左/横排从左到右） |
| `add_seal(canvas, position, size)` | 添加红色印章 |

---

## 五、数据流说明

### 5.1 训练数据流

```
原始图片(.jpg/.png)
    ↓
load_and_preprocess (灰度→去噪→二值化→裁切→缩放)
    ↓
extract_skeleton (Zhang-Suen细化→膨胀)
    ↓
augment (旋转/弹性形变/亮度/模糊 × N)
    ↓
CalligraphyDS → DataLoader
    ↓
StyleEncoder(图片) → 风格向量
ContentEncoder(骨架) → 内容特征
    ↓
DualBranchGenerator(内容特征, 风格向量) → 生成图片
    ↓
Discriminator → 真假判别 + 风格分类
    ↓
CalligraphyLoss → 反向传播更新参数
```

### 5.2 推理/生成数据流

```
用户输入文本 + 风格选择
    ↓
[1] 字库匹配 → Calli-Tongji 查找
    ↓ 未命中
[2] GAN推理 → StyleEncoder(参考图) + ContentEncoder(骨架) → Generator
    ↓ 模型不可用
[3] Canvas模拟 → PIL字体渲染 + 8步后处理
    ↓
StrokeRenderer.compose_work → 竖排/横排合成
    ↓
宣纸纹理 + 印章 → PNG → Base64
    ↓
前端显示/下载
```

---

## 六、启动与使用

### 6.1 环境依赖

```
Python 3.x
PyTorch (CUDA 12.x)
FastAPI + Uvicorn
OpenCV (opencv-python + opencv-contrib-python)
Pillow
numpy
```

### 6.2 启动方式

**启动服务**:
```bash
python algorithm/server/app.py
# 服务地址: http://localhost:8080
```

**训练模型**:
```bash
# 6风格联合训练
python algorithm/train/run_train.py

# 米芾单风格训练
python algorithm/train/train_mifu.py

# 从checkpoint继续训练
python algorithm/train/continue_train.py
```

### 6.3 访问系统

浏览器打开 `http://localhost:8080` 即可进入系统。

---

## 七、支持的书法风格

| 风格 | 书体 | 朝代 | 特点 | 系统Key |
|------|------|------|------|---------|
| 米芾 | 行书 | 宋代 | 沉着痛快，八面出锋 | mifu |
| 赵孟頫 | 楷书 | 元代 | 圆润秀美，流畅自然 | zhaomf |
| 褚遂良 | 楷书 | 唐代 | 清朗秀劲，灵动飘逸 | chushl |
| 乙瑛碑 | 隶书 | 汉代 | 婉畅飘逸，遒劲古拙 | yybei |
| 邓石如 | 篆书 | 清代 | 圆转匀称，刚健婀娜 | dengsr |
| 怀素 | 草书 | 唐代 | 狂放飘逸，如骤雨旋风 | huaisu |
